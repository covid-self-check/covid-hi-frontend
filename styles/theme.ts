import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3E54D3",
      light: "#4F80E2",
    },
    secondary: {
      main: "#15CDCA",
      light: "#4FE0B6",
    },
  },
});

export default theme;
